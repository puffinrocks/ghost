version: '2'

services:
 
    main:
        image: puffinrocks/ghost:0.11.4
        ports: 
            - 2368
        environment:
            - VIRTUAL_HOST
            - VIRTUAL_PORT=2368
        networks:
            - front
            - back
            - default
        volumes: 
            - main_data:/var/lib/ghost
        tmpfs:
            - /run
            - /tmp

volumes:
    main_data:

networks:
    front:
        external: 
            name: "puffin_front"
    back:
        external:
            name: "puffin_back"
